<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.harpreet.okhlee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".volly.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewsList"
            android:label="@string/title_activity_news_list"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".news_web"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Youtube_Comment"
            android:label="@string/title_activity_youtube__comment"
            android:noHistory="true"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".News_Description"
            android:label="@string/title_activity_news__description"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>